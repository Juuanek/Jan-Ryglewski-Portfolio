Create application that can store some objects as ArrayList, can display them,
show description about the object
addItem to ArrayList. You can create 2 or 3 objects to make sure everything works fine.